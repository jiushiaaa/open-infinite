# 未终章 v0.1-v0.8 版本审计

> 更新时间：2026-05-30；2026-06-01 标注后续状态。<br>
> 用途：保留 v0.1 到 v0.8 阶段的已完成能力、当时产物和当时未做项。本文是历史审计快照，不承担当前路线图职责；当前事实、暂停点和真实未做项以 `../../memory.md` 与 `../living-novel-engine-iteration-plan.md` 为准。

## 总览

| 版本段 | 已完成主能力 | 当前判断 |
| --- | --- | --- |
| Phase 0 / v0.1 | CLI 干预、三分支、`resume continue`、`resume intervene`、run lineage 与基础 artifact | 概念验证闭环已完成，可继续作为内置演示样例 |
| v0.2 | 文本导入、项目结构、真实 LLM 双 pass 抽取、imported 项目 resume、genre templates / facts / summaries / story contract | 用户自有文本入口已完成，长篇质量增强进入 v0.8 |
| v0.3 | BM25 Lite、章节距离衰减、facts/summaries/contract 检索注入、`retrieval_context.json` | 轻量检索已进入运行时，向量库和 reranker 延后 |
| v0.4 | `lne browse` 只读浏览器、路径安全、空态降级、检索记忆展示 | 开发者/演示 viewer 完成，普通用户前端已由 v0.7 承接 |
| v0.5 | 第四面墙账本、角色觉察、决策/叙事注入、关闭语义 | LNE 差异化机制已可演示，前端展示可后续补 |
| v0.6 | SceneRunner adapter、多 Agent 协议、stub/LLM runner、trace 可视化、质量校验/重试/usage | 多 Agent 底座完成，默认 runner 仍保持 `lightweight` |
| v0.7.1 | Intervention Compiler、LLM compiler fallback、Causal Diff 后端 artifact | 自由输入不再被固定三分支心智锁死 |
| v0.7 | React/Vite 产品 Web App 九刀：导入、创世、锚定、干预、Causal Diff、设置、异步 Job | 普通用户主闭环完成 |
| v0.7.2 | CharacterAction、CharacterProbe、InterventionGuardrail、角色探针与预检 UI | Agent Interaction 第一层完成 |
| v0.7.3 | Seedream 视觉资产增强层、封面/头像/场景图、无 Key 占位降级 | 视觉增强完成，不阻塞文字主链路 |
| v0.7.4 | Baseline Worldline、Canon Holdout、Canon Replay deterministic evaluator 与 Web 区块 | 无干预对照与正史回放完成 |
| v0.7.5 | Worldline Judge、branch 级评分、故事弧/转折/张力、工作台评审标签 | 世界线选择辅助完成 |
| v0.8.0-A 至 v0.8.5-A | `source_raw/`、`import_report.json`、`memory/`、`canon_ledger.jsonl`、账本检索、`consistency_report.json`、holdout visibility manifest | Long Novel Memory artifact 底座完成 |
| v0.8+ ActDirector-A | `act_director_plan.json`，抽象干预到角色动作计划 | 规划 artifact 完成，暂不驱动 runner |
| v0.8+ Discourse-aware Narrator-A | `narrative_diagnostics.json`，正文节奏/转折/张力写后诊断 | 诊断 artifact 完成，暂不反馈 narrator |
| v0.8+ Dynamic Action Registry-A | `dynamic_action_registry.yaml`，动作类型/别名/前置/效果/失败/修复汇总 | 动作注册表 artifact 完成，暂不执行状态变化 |
| v0.8+ Emergence Mining-A | `emergence_nodes.json`，run 级候选涌现节点汇总与 API | 涌现节点沉淀完成，暂不做推荐系统 |

## v0.8 当前产物边界

v0.8 已形成一条完整的“长篇记忆 + 行动规划 + 质量/涌现审计”artifact 链：

```text
import long novel
  -> source_raw/
  -> import_report.json
  -> memory/
     -> memory_manifest.json
     -> canon_ledger.jsonl
     -> consistency_report.json
  -> retrieval_context.json（canon_ledger source）

intervene
  -> act_director_plan.json
  -> dynamic_action_registry.yaml
  -> branch_*/narrative_diagnostics.json
  -> branch_*/worldline_judgement.json（按需生成）
  -> emergence_nodes.json
```

这些新增产物全部是 additive：不改变 `run_scene` 默认行为，不破坏 `chapter.md`、`events.json`、`state_snapshot.json`、`multi_agent_trace.json`、`causal_diff.json` 的既有契约。

## 当时未完成项的后续状态

以下表格保留 2026-05-30 审计时的缺口，并标注 2026-06-01 的后续状态，避免把历史待办误判为当前任务。

| 方向 | 2026-05-30 未做项 | 2026-06-01 后续状态 |
| --- | --- | --- |
| 长篇导入产品化 | 前端分片上传、断点续传、epub/zip、后台部分索引进度 | 主要入口已完成：txt/md/zip/epub、服务端 ingest session、断点续传、job 进度和失败空态已收口；云端持久队列仍后置。 |
| 长篇语义抽取 | LLM scene 级切分、细粒度事件抽取、角色状态投影、章节摘要重写 | 分层记忆、canon ledger、entity aliases、MasterSetting Workspace Lite 已完成底座；ChapterBrief/scene 级 LLM 摘要质量增强仍是后续候选。 |
| 检索增强 | entity alias resolution、entity index、向量库、embedding、reranker、prompt budget pack | entity aliases、canon ledger retrieval、runtime memory consumption、retrieval probes 已完成；向量库/embedding/reranker 仍按触发式评估后置。 |
| runner 消费 | runner 读取 memory 层、`act_director_plan`、`dynamic_action_registry`、`emergence_nodes` 并影响下一轮推演 | runtime memory 已只读消费；状态执行 overlay 支持 opt-in apply/rollback；overlay 自动喂回下一轮 runner 仍未做。 |
| 叙事生成升级 | outline -> turning points -> chapter 两阶段 narrator、诊断结果反馈到下一轮生成 | `narrative_diagnostics.json` 与 Worldline Judge 已提供诊断/评审；诊断自动反馈 narrator 仍未做。 |
| 前端 artifact 面板 | import report、memory manifest、consistency report、visibility manifest、act director、dynamic action、narrative diagnostics、emergence nodes 的产品展示 | 已收束进长篇项目工作台与右侧「机制档案」。 |
| 长篇评估 | 批量 holdout replay UI、运行后写回审计、跨章节一致性趋势 | range replay、replay audit UI、post-run audit entry 已完成；运行后审计写入正史账本仍未做。 |
| 涌现产品化 | 跨 run 聚类、高价值节点推荐、世界线模板、社区/分享 | `emergence_nodes.json` 已沉淀 run 级节点；跨 run 聚类、推荐系统、社区/分享仍后置。 |
| 商业化硬化 | 多 provider gateway、成本预算、队列、权限、多租户、版权策略 | Provider/Cost Lite、权限矩阵草案、版权/保留/审计、本地部署与商业化边界已完成；真实云端多租户、对象存储、认证、计费仍后置。 |

## 历史下一步建议的处理结果

2026-05-30 曾建议继续 v0.8.x，优先级如下；截至 2026-06-01 的处理结果为：

1. `entity_aliases.yaml` + entity resolution：已完成，并进入检索归一化与世界锚定展示。
2. 前端 v0.8 artifact 面板：已完成，并统一为右侧「机制档案」与长篇项目工作台资产展示。
3. runner consumption 第一刀：已完成只读运行记忆消费；低风险状态执行改为 opt-in overlay apply/rollback，自动消费仍后置。
4. 长篇上传产品化：已完成主要入口，支持 txt/md/zip/epub、分片、断点续传、job 进度与失败空态。

原建议“不立刻做 Zep / OASIS / CAMEL / LangGraph 替换主线”仍然有效。v0.9.3 / v0.9.4 已完成触发式评估和 probe，当前仍不接重依赖，除非真实失败样例证明现有 BM25/ledger/alias 或自研 runner 不足。
