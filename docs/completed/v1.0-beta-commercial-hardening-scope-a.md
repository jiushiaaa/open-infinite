# v1.0-beta Commercial Hardening Scope-A

> 收口日期：2026-06-01
> 性质：商业化加固范围复核第一刀，只读 scope report，不进入云端平台重构。

## 目标

在 v0.9.x 长篇共创闭环和真实使用压力增强完成后，先把 v1.0-beta 的商业化加固边界讲清楚，避免直接跳到多租户账号、对象存储、计费系统或云端队列。

## 已完成

- 新增 `service.commercial_hardening.get_commercial_hardening_scope()`。
- 新增只读 API：`GET /api/settings/commercial-hardening-scope`。
- 报告覆盖七个商业化域：
  - 账号与项目空间
  - 权限模型
  - 云端持久化
  - 配额与成本护栏
  - 审计日志
  - 版权与分享边界
  - 部署与观测
- 每个域返回当前覆盖、缺口、本地优先下一步、平台化下一步、风险等级和决策。
- 报告明确三类延后项：多租户账号、云端对象存储、商业计费系统。
- 报告列出下一刀候选：本地 `project_audit_log.jsonl` schema、权限矩阵草案、项目级版权/来源声明。

## 边界

- 不创建客户端、不联网、不读取或回显密钥。
- 不写 artifact，不改变任何 run/project 产物。
- 不接真实账号、团队空间、多租户权限、对象存储、数据库、持久队列或计费系统。
- 不改 `run_scene` 默认行为。
- 不改变 `chapter.md`、`events.json`、`state_snapshot.json`、`multi_agent_trace.json`、`causal_diff.json` 契约。

## 验收

- Focused：`python -m pytest tests/test_runtime_settings_api.py -q` → `27 passed`
- Full backend：`python -m pytest -q` → `645 passed`
- Frontend：`cd engine/ui && pnpm run build` → 通过
- Diff：`git diff --check` → 通过

## 下一步

后续 `v1.0-beta Commercial Audit Log Schema-B`、`v1.0-beta Permission Matrix Draft-C` 与 `v1.0-beta Project Copyright Statement-D` 已收口。继续进入 `v1.0-beta Quota & Observability Lite-E`：基于现有 provider usage、job 状态和本地 artifact 做配额/观测口径。
