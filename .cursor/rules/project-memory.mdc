---
description: LNE 四文档上下文——会话开始读 memory/迭代计划/PRD/engine README；任务结束只写 memory.md
alwaysApply: true
---

# LNE 项目上下文（四文档）

Living Novel Engine 的跨会话事实来源是**四份文档**，不是只靠 `memory.md` 或聊天历史。多会话并行时必须依赖它们。

| 文档 | 路径 | 职责 |
|------|------|------|
| **项目记忆** | `memory.md` | 跨会话快照：当前状态、已知缺口、官方路线、变更日志（**唯一写入目标**） |
| **迭代计划** | `docs/living-novel-engine-iteration-plan.md` | 版本路线图、执行顺序、近期任务清单、数据结构演进 |
| **产品 PRD** | `docs/living-novel-engine-prd.md` | 产品定位、核心概念、用户流程、功能/页面需求、验收标准 |
| **Engine README** | `engine/README.md` | CLI 用法、版本表、安装配置、输出结构、测试命令 |

## 会话开始（第一件事）

在回答用户、写代码或做规划之前，若任务与 LNE / `engine/` 相关：

1. **并行**用 Read 工具读取上表四份文档（不要只读 `memory.md`）。
2. 按任务类型抓重点章节（不必每次全文通读，但四份都要打开）：

| 文档 | 默认必读 | 按任务加读 |
|------|----------|------------|
| `memory.md` | §3 当前状态、§6 已知缺口、§7 规划路线 | §9 设计决策、§10 模块索引、§11 验收 run_id |
| `docs/living-novel-engine-iteration-plan.md` | §2 当前状态总览、§5 版本路线图、§7 推荐执行顺序 | §8 近期任务清单；做 schema/artifact 时读 §9 |
| `docs/living-novel-engine-prd.md` | §3 产品定位、§6 核心概念 | 做功能/UI/流程时读 §8–§10、§13 验收标准 |
| `engine/README.md` | 版本表、测试基线 | 跑 CLI/改输出/联调时读「快速演示」「输出结构」「配置」 |

3. **事实优先级**（冲突时）：`memory.md` > `living-novel-engine-iteration-plan.md` > `engine/README.md` > `living-novel-engine-prd.md`。PRD 偏产品愿景，以 `memory.md` 的收口状态为准。
4. 用户任务**明显与 LNE / `engine/` 无关**时可跳过四文档。

## 任务结束后（必须做）

每当完成一轮**有意义的**开发、设计收口、验收或文档更新（含仅改文档、仅加规则）：

1. 只更新 **`memory.md`**（不要直接改迭代计划/PRD/README 来记进度；大计划变更应走正常文档 PR）：
   - 在 **§13 变更日志** 末尾追加一条，格式见该文件 **§12 Agent 维护说明**
   - 视情况同步 **§3** 测试基线/下一版、**§4** 已收口版本、**§6** 缺口勾选、**§7** 待办勾选
   - 更新文首 **「最后更新」** 日期
2. 不要在 `memory.md` 里粘贴大段代码或整段 plan；用文件路径引用即可。

## 变更日志条目模板

```markdown
### YYYY-MM-DD — 简短标题
- **做了什么**：（1–3 条）
- **测试**：（如有，如 442 passed）
- **文件**：（关键路径）
- **下一刀建议**：（可选）
```

## 注意

- 用户 @`memory.md`、@迭代计划/PRD/README，或说「先读 memory」时，**仍应读取完整四文档**，不要只打开 `memory.md`。
- 未完成实质工作、仅闲聊时，不必写变更日志。
