---
description: 会话开始读 memory.md，任务结束后更新跨会话项目记忆
alwaysApply: true
---

# 项目记忆（memory.md）

本仓库根目录有 **`memory.md`**，用于跨会话保留 Living Novel Engine 的完成项、缺口与路线。多会话并行时必须依赖它，不要只靠聊天历史。

## 会话开始（第一件事）

在回答用户、写代码或做规划之前：

1. 用 Read 工具读取仓库根目录 **`memory.md`**（路径：`memory.md`）。
2. 以其中的 **§3 当前状态**、**§6 已知缺口**、**§7 规划路线** 为事实来源；若与聊天记忆冲突，以 `memory.md` 为准。
3. 若用户任务明显与 LNE / `engine/` 无关，可跳过；否则默认必读。

## 任务结束后（必须做）

每当完成一轮**有意义的**开发、设计收口、验收或文档更新（含仅改文档、仅加规则）：

1. 打开 **`memory.md`**，在 **§13 变更日志** 末尾追加一条，格式见该文件 **§12 Agent 维护说明**。
2. 视情况同步更新：
   - **§3** 测试基线、官方下一版
   - **§4** 若新版本已收口
   - **§6** 勾选已解决的缺口
   - **§7** 勾选已完成待办
3. 更新 **文首「最后更新」日期**。
4. 不要在 `memory.md` 里粘贴大段代码；用文件路径引用即可。

## 变更日志条目模板

```markdown
### YYYY-MM-DD — 简短标题
- **做了什么**：（1–3 条）
- **测试**：（如有，如 174 passed）
- **文件**：（关键路径）
- **下一刀建议**：（可选）
```

## 注意

- 用户说「先读 memory」或 @`memory.md` 时，立即读取并遵守。
- 未完成实质工作、仅闲聊时，不必写变更日志。
